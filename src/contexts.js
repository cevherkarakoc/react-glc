import React from 'react';

export const AnimationFrameContext = React.createContext(null);
export const CanvasContext = React.createContext(null);
export const MeshAssetContext = React.createContext(null);
export const ProgramContext = React.createContext(null);
